var score = 0;
var bird_position = 0;
var pipes = [
    { 'x': }
]

const delta_pipe = 10; 

function gameStart() {
    //TODO: Start the game
}

//Game Loop
function gameLoop() {
    // Read Inputs
    
    // Update Positions

    //Redraw Screen 
    setTimeout(gameLoop, 10);
}

function gameOver() {
    //TODO: Popup "Game Over" Screen 
    //      Button to restart the game 
}